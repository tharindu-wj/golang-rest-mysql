{
    "version": 2,
    "name": "Test API Gateway",
    "port": 8000,
    "cache_ttl": "3600s",
    "timeout": "3s",
    "extra_config": {},
    "output_encoding": "json",
    "endpoints": [
        {
            "endpoint": "/abc/{id}",
            "backend": [

              {
                "url_pattern": "/user/{id}",
                "host": [
                  "http://localhost:8081"
                ]
              }
            ]
          },
      {
        "endpoint": "/posts",
        "backend": [

          {
            "url_pattern": "/posts",
            "host": [
              "http://localhost:8081"
            ]
          }
        ]
      }
    ]
}
